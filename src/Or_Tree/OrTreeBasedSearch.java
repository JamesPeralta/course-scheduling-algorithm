package Or_Tree;
import DataStructures.*;
import java.util.ArrayList;

public class OrTreeBasedSearch {
    public static void generateSample(Department department){
        ArrayList<Course> courses = department.getCourses();
        ArrayList<CourseSlot> courseSlots = department.getCourseSlots();
        ArrayList<Lab> labs = department.getLabs();
        ArrayList<LabSlot> labSlots = department.getLabSlots();

        Prob newInstance = new Prob(department);
    }

    private void erw(){

    }
}
